export interface Product {
  id: number;
  name: string;
  description: string;
  image: string;
  price: string;
  cpu: string;
  cpu_brand: string;
  gpu: string;
  gpu_brand: string;
  ram: string;
  ram_size: string;
  storage: string;
  storage_size: string;
}